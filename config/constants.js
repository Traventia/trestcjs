

module.exports = Object.freeze({
	//By default, 60 seconds of tiemout if is not especificly...
	DEFAULT_TIMEOUT : 60000,

	//By default we only allow 5 redirection depth
	MAX_REDIRECTION_DEPTH : 5,

	//Default log if not indicated
	LOG_BY_DEFAULT : false
});


